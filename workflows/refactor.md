# Refactor workflow

1. Preserve existing article URLs.
2. Keep `public/llms.txt` and `public/llms-full.txt` aligned with important
   posts.
3. Update tags when moving topic clusters.
4. Run `npm run build`.
