# Implementation workflow

1. Use approved frontmatter from `POSTING.md`.
2. Include a direct answer near the top.
3. Add comparison tables or FAQ sections when useful.
4. Avoid unsupported claims and absolute rankings.
5. Run `npm run build`.
