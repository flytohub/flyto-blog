{
  "name": "flyto-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "test": "node scripts/audit-blog-source.mjs",
    "lint": "node --check scripts/audit-blog-source.mjs && node --check scripts/audit-seo-surface.mjs",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "audit:seo": "node scripts/audit-seo-surface.mjs",
    "verify": "npm run test && npm run lint && npm run build && npm run audit:seo"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
